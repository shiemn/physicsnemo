# @package _global_
hydra:
  searchpath:
    - pkg://conf/base
defaults:
  - base
  - _self_
target: wind
stage: regression
dataset:
  out_channels: [2, 3]
generation:
  inference_mode: regression
  hr_mean_conditioning: false
  num_ensembles: 1
  seed_batch_size: 1
  io:
    res_ckpt_filename: null
